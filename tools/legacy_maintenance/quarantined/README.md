# Quarantined scripts

These unreferenced scripts were removed from the runtime/importable paths during the application audit. `seed_all_doctypes.py` mass-populated business data with sample values, and `test_order.py` created a hard-coded order. Neither should run in production. They are retained here for review only.
